import 'package:flutter/material.dart';

class Quizly extends StatelessWidget {
  const Quizly({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
    );
  }
}